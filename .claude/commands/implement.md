---
description: Phase 5 - Implement one approved task from impl-plan.md via the implementer subagent
---

Use the implementer subagent to implement task ID "$ARGUMENTS" from
docs/impl-plan.md. Confirm the task's dependencies are already done
before starting. Run build/test/lint before reporting completion, and
report exactly what was done, what tests were added, and anything left
as `Not Found` / blocked.
